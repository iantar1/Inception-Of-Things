apiVersion: v1

kind: Service

metadata:
  name: app3-service

spec:
  selector:
    app: app3
  # type: NodePort
  ports:
    - protocol: TCP
      # nodePort: 30003
      port: 80
      targetPort: 80