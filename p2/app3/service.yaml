apiVersion: v1

kind: Service

metadata:
  name: app3-service

spec:
  selector:
    app: app3
  type: NodePort
  ports:
    - protocol: TCP
      nodePort: 30003
      port: 30003
      targetPort: 80

      # /usr/share/nginx/html